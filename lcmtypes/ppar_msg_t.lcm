package ppar;

struct msg_t {
    int64_t utime;

    int8_t RE_VERSION;

    //fingerprint the outgoing request -- receive message of same type with same fingerprint
    int32_t requeID;
    string  requeOrig;
    string  replyOrig;

    string datastr;
}
